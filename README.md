# 🪙 加密货币价格追踪器

使用 **GitHub Pages** 免费托管的实时加密货币价格监控应用

## ✨ 项目特点

```
✅ 完全免费部署（GitHub Pages）
✅ 实时数据更新（30秒自动刷新）
✅ 美观的响应式UI
✅ 支持8种主流加密货币
✅ 显示USD和CNY双币种价格
✅ 24小时涨跌幅展示
✅ 市值实时显示
```

## 🚀 技术栈

| 分类 | 技术 | 说明 |
|------|------|------|
| **前端框架** | React 18 | 现代化UI |
| **构建工具** | Vite 5 | 快速开发构建 |
| **托管平台** | GitHub Pages | 免费静态网站托管 |
| **数据源** | CoinGecko API | 免费加密货币价格API |
| **部署** | GitHub Actions | 自动化CI/CD |

## 📦 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/你的用户名/crypto-price-tracker.git
cd crypto-price-tracker
```

### 2. 安装依赖

```bash
npm install
```

### 3. 本地开发

```bash
npm run dev
```

访问 http://localhost:5173

### 4. 构建生产版本

```bash
npm run build
```

## 🌐 部署到GitHub Pages

### 方法1: 使用GitHub Actions（推荐）

1. **创建GitHub仓库**
   ```bash
   # 初始化Git
   git init
   git add .
   git commit -m "Initial commit"

   # 推送到GitHub（替换为你的仓库地址）
   git remote add origin https://github.com/你的用户名/crypto-price-tracker.git
   git branch -M main
   git push -u origin main
   ```

2. **配置GitHub Pages**
   - 进入仓库 Settings
   - 左侧菜单选择 **Pages**
   - Source选择 **GitHub Actions**
   - 保存

3. **自动部署**
   - 每次推送到main分支会自动触发部署
   - 等待1-2分钟
   - 访问 `https://你的用户名.github.io/crypto-price-tracker/`

### 方法2: 手动部署

```bash
# 构建并部署
npm run build
npm run deploy
```

## 📝 修改为你自己的项目

1. **修改vite.config.js**
   ```js
   export default defineConfig({
     plugins: [react()],
     base: '/你的仓库名/',  // ⚠️ 改为你的仓库名
   })
   ```

2. **修改package.json**
   ```json
   {
     "name": "你的项目名",
     // ...
   }
   ```

3. **推送更新**
   ```bash
   git add .
   git commit -m "Update config"
   git push
   ```

## 🎨 功能展示

### 实时价格监控
- 8种主流加密货币（BTC、ETH、BNB、ADA、SOL、DOT、DOGE、XRP）
- 实时USD和CNY价格
- 30秒自动刷新

### 数据统计
- 24小时价格变化百分比
- 实时市值（Billion/Million/Thousand格式化）
- 涨跌颜色标识（绿涨红跌）

### 响应式设计
- 桌面端：网格布局（最多4列）
- 移动端：单列布局
- 流畅的悬停动画

## 🔧 自定义配置

### 添加更多加密货币

编辑 `src/App.jsx`：

```jsx
const cryptoList = [
  { id: 'bitcoin', name: 'Bitcoin', symbol: 'BTC', icon: '₿' },
  // 添加更多...
  { id: 'your-coin-id', name: 'Your Coin', symbol: 'SYMBOL', icon: 'X' },
]
```

### 修改刷新间隔

```jsx
// src/App.jsx
const interval = setInterval(fetchCryptoPrices, 30000) // 改为你想要的毫秒数
```

## 🌍 免费服务使用说明

### CoinGecko API
- **免费额度**: 每月10,000-50,000次请求
- **限制**: 每分钟10-50次
- **无需注册**: 直接使用
- **文档**: https://www.coingecko.com/en/api

### GitHub Pages
- **存储**: 1GB（软限制）
- **流量**: 100GB/月（软限制）
- **构建时间**: 10分钟超时
- **完全免费**: 无需信用卡

## 📂 项目结构

```
crypto-price-tracker/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions部署配置
├── src/
│   ├── App.jsx                 # 主应用组件
│   ├── App.css                 # 应用样式
│   ├── main.jsx                # React入口
│   └── index.css               # 全局样式
├── index.html                  # HTML模板
├── vite.config.js              # Vite配置
├── package.json                # 依赖配置
└── README.md                   # 项目文档
```

## 🐛 常见问题

### 1. 部署后页面空白

**原因**: `vite.config.js` 中的 `base` 路径不匹配

**解决**:
```js
// vite.config.js
export default defineConfig({
  base: '/你的仓库名/',  // 必须与GitHub仓库名一致
})
```

### 2. API请求失败

**原因**: CoinGecko API限流

**解决**:
- 增加刷新间隔（从30秒改为60秒）
- 检查网络连接
- 等待几分钟后重试

### 3. GitHub Actions部署失败

**检查清单**:
- [ ] Settings → Pages → Source 是否选择了 "GitHub Actions"
- [ ] .github/workflows/deploy.yml 文件是否存在
- [ ] package.json 中依赖版本是否正确
- [ ] 查看Actions标签页的错误日志

## 📊 性能优化

- ✅ Vite快速构建（<10秒）
- ✅ React组件懒加载
- ✅ API请求防抖（30秒）
- ✅ CSS动画硬件加速
- ✅ 响应式图片加载

## 🔮 未来扩展

如需更多功能，可以免费集成：

| 功能 | 推荐服务 | 免费额度 |
|------|---------|---------|
| **数据库** | Supabase | 500MB存储 |
| **用户认证** | Clerk | 10,000 MAU |
| **API后端** | Vercel Serverless | 100GB流量/月 |
| **图片存储** | Cloudflare R2 | 10GB免费 |

## 📄 许可证

MIT License - 随意使用和修改

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📮 联系方式

- GitHub: [@你的用户名](https://github.com/你的用户名)
- 项目地址: https://github.com/你的用户名/crypto-price-tracker

---

**⭐ 如果这个项目对你有帮助，请给个Star！**
